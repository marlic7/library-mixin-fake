var _ = require('underscorek-extend');

module.exports = {
	test: function(expectVer) {
		var check = (_.version == expectVer ? true : false);
		log('invoked test from library-abc with underscorek version: ' + 
				_.version + (check ? '' : ' (expected: ' + expectVer + ')'), 
			  (check ? 'green' : 'red'),
				__filename);
	}
}

log('module library-xyz loaded', 'reset', __filename);