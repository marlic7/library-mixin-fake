// remove underscore from cache (because we would like to extend new object and not the reference from cache)
try {
    var rememberedCache = require.cache[require.resolve('underscorek')];
    delete require.cache[require.resolve('underscorek')];
} catch (e) {}

var _ = require('underscorek');

// restore original cache state
require.cache[require.resolve('underscorek')] = rememberedCache;

_.version = _.version + '-ext-2.0.0';

module.exports = _;

log('module underscorek-extend loaded: 2.0.0', 'reset', __filename);

